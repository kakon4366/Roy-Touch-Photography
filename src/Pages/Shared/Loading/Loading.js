import React from "react";

const Loading = () => {
	return (
		<div className="absolute w-screen h-screen flex justify-center items-center">
			<h2 className="text-6xl text-red-500">Loading....</h2>
		</div>
	);
};

export default Loading;
